const Products = () => {
  // console.log(userData);

  return (
    <div>
      Products
      {/* <p>{userData?.name}</p>
      <p>{userData?.name}</p> */}
    </div>
  );
};

export default Products;
// { userData }: any
